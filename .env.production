# Production API base — the deployed FastAPI backend on Vercel.
# The frontend calls `${VITE_API_BASE_URL}/api/...` directly (no dev proxy in production).
VITE_API_BASE_URL=https://server-aivoa.vercel.app
